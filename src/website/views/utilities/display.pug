---
title: Display
---

p.-text Use shorthand display utility classes to quickly change an elements display value.

h3 Notation
p.-text
  | Display utility classes can be defined using the format <code>-d-{breakpoint}--{value}</code>.
  | Breakpoints supported are <code>sm</code>, <code>md</code>, <code>lg</code>, and <code>xl</code>.

h4 Target all breakpoints
p.-text Display utility classes with no breakpoint defined apply to all screen sizes.
section.a-table.-bordered.-mt3.-mb3
  div
    table.-text(cellpadding='0', cellspacing='0')
      thead
        tr
          th
            div Display Class
          th
            div Value
      tbody
        each type in ['none', 'inline', 'inline-block', 'block', 'table', 'table-cell', 'table-row', 'flex', 'inline-flex']
          tr
            td(width='40%')
              code= '-d--' + type
            td
              code= 'display: ' + type + ';'

h4 Target specific breakpoints
p.-text
  | To target a specific breakpoint, add the breakpoint abbreviation to the class. As a mobile first framework, it will
  | apply to that specific breakpoint and up.
section.a-table.-bordered.-mt3.-mb3
  div
    table(cellpadding='0', cellspacing='0')
      thead
        tr
          th
            div Display Class
          th
            div Value
      tbody
        each type in ['sm', 'md', 'lg', 'xl']
          tr
            td.-text(width='40%')
              code= '-d-' + type + '--none'
            td.-text Hidden only on <code>#{type}</code>

h3 Examples
.example.-mb3
  .-p3
    .-text.-b1.-p3.-m1.-d--none.-d-xl--inline-flex(style="background:#E4F2CE;border-color:#C6DBA7;") -d--none -d-xl--inline-flex
    .-text.-b1.-p3.-m1.-d--flex.-d-sm--none(style="background:#E4F2CE;border-color:#C6DBA7;") -d-sm--none
    .-text.-b1.-p3.-m1.-d--inline-flex(style="background:#E4F2CE;border-color:#C6DBA7;") -d--inline-flex
    .-text.-b1.-p3.-m1.-d--flex(style="background:#E4F2CE;border-color:#C6DBA7;") -d--flex
  :code(lang="html")
    <!-- hidden on all breakpoints except xl -->
    <div class="-d--none -d-xl--inline-flex"></div>
    <!-- hidden on sm breakpoints -->
    <div class="-d-sm--none"></div>
    <!-- inline-flex on all breakpoints -->
    <div class="-d--inline-flex"></div>
    <!-- flex on all breakpoints -->
    <div class="-d--flex"></div>
