---
title: Tooltip
---

p.-text
  | Tooltips provide text labels that display when users hover or focus on an element.

h3 Base
p.-text
  | To render a tooltip, apply the <code>data-tooltip</code> attribute to an element. Then,
  | you can initialize it with the chi.tooltip factory method.
.example.-mb--3
  .-p--3
    button(data-tooltip='Your tooltip text on a button').a-btn.-mr--2 Tooltip
  :code(lang='html')
    <button id='tooltip-1' class="a-btn" data-tooltip="Your tooltip text on a button">Tooltip</button>
    <script>chi.tooltip(document.getElementById('tooltip-1'));</script>

h3 Positioning
p.-text
  | By default, tooltip's position on top of an element. To alter position, use
  | the <code>data-position</code> attribute. Valid values are <code>top</code>,
  | <code>right</code>, <code>bottom</code>, <code>left</code>. You can pass an array
  | of Elements and initialize all at once.
.example.-mb--3
  .-p--3
    button(data-tooltip='Your top tooltip text').a-btn.-mr--2.-mb--2.-mb-md--0 Top Tooltip
    button(data-tooltip='Your right tooltip text', data-position='right').a-btn.-mr--2.-mb--2.-mb-md--0 Right Tooltip
    button(data-tooltip='Your bottom tooltip text', data-position='bottom').a-btn.-mr--2.-mb--2.-mb-md--0 Bottom Tooltip
    button(data-tooltip='Your left tooltip text', data-position='left').a-btn.-mr--2.-mb--2.-mb-md--0 Left Tooltip
  :code(lang='html')
    <button class="a-btn" data-tooltip="Your top tooltip text">Top Tooltip</button>
    <button class="a-btn" data-tooltip="Your right tooltip text" data-position="right">Right Tooltip</button>
    <button class="a-btn" data-tooltip="Your bottom tooltip text" data-position="bottom">Bottom Tooltip</button>
    <button class="a-btn" data-tooltip="Yourleft tooltip text" data-position="left">Left Tooltip</button>
    <script>chi.tooltip(document.querySelectorAll('[data-tooltip])');</script>

h4 Preventing memory leaks
p.-text
  | Tooltip components have a dispose function to free all resources attached to the element, such as event listeners
  | and object data. You must call this method when you want to remove the component.
:code(lang="javascript")
  var elem = document.getElementById('tooltip');
  var tooltip = chi.tooltip(elem);
  // do stuff
  tooltip.dispose();

p.-text
  span.a-badge.-primary.-mr1
    span Tip
  | It is safe to call the <code>tooltip</code> method more than once, as it will return any previously created tooltip component
  | associated to the element.
:code(lang="javascript")
  var elem = document.getElementById('tooltip-1');
  var tooltip = chi.tooltip(elem);
  var elem2 = document.getElementById('tooltip-1');
  var tooltip2 = chi.tooltip(elem2);
  tooltip === tooltip2; // returns true

  tooltip.dispose(); // Only have to do it once.

script.
  document.addEventListener(
    'DOMContentLoaded',
    function() {
      chi.tooltip(document.querySelectorAll('[data-tooltip]'));
    },
    false
  );
