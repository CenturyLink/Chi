---
title: Tables
---
p.-text Tables are used to present data sets in a simple and organized way.

h3 Requirements
p.-text
  | Tables must be wrapped in a <code>&lt;section class="a-table"&gt;</code>
  | with a nested <code>&lt;div&gt;</code>. Table header text must be
  | wrapped in a <code>&lt;div&gt;</code>.
h3 Examples
h4 Base
:example(lang="html"):html-beautify:pug include ./_default.pug

h4 Scrollable
p.-text
  | If the <code>section</code> height is smaller than the <code>table</code>
  | height, the <code>table</code> header will be fixed positioned and
  | content will scroll.
:example(lang="html"):html-beautify:pug include ./_scroll.pug

h4 Bordered
p.-text Tables can be bordered by applying the modifier class <code>-bordered</code>.
:example(lang="html"):html-beautify:pug include ./_bordered.pug

h4 Borderless
p.-text Remove all borders by applying the modifier class <code>-borderless</code>.
:example(lang="html"):html-beautify:pug include ./_borderless.pug

h4 Striped
p.-text Alternate the table row color by applying the modifier class <code>-striped</code>.
:example(lang="html"):html-beautify:pug include ./_striped.pug

h4 Hover
p.-text Enable a hover state by applying the modifier class <code>-hover</code>.
:example(lang="html"):html-beautify:pug include ./_hover.pug

h3 Row States
p.-text
  | Chi offers a wide variety of modifiers to express different row states.

h4 Disabled
p.-text Disable a row by applying the attribute <code>disabled="disabled"</code>.
:example(lang="html"):html-beautify:pug include ./_disabled.pug

h4 Success
p.-text Render a success state by applying the modifier class <code>-rowSuccess</code>.
:example(lang="html"):html-beautify:pug include ./_row-success.pug

h4 Warning
p.-text Render a warning state by applying the modifier class <code>-rowWarning</code>.
:example(lang="html"):html-beautify:pug include ./_row-warning.pug

h4 Danger
p.-text Render a danger state by applying the modifier class <code>-rowDanger</code>.
:example(lang="html"):html-beautify:pug include ./_row-danger.pug

h4 Info
p.-text Render an info state by applying the modifier class <code>-rowInfo</code>.
:example(lang="html"):html-beautify:pug include ./_row-info.pug

h3 Column Sorting
p.-text
  | To express column sorting, apply the class <code>-sorted</code> with one of
  | the following modifiers: <code>-ascending</code> or <code>-descending</code>.

h4 Ascending
:example(lang="html"):html-beautify:pug include ./_sort-ascending.pug

h4 Descending
:example(lang="html"):html-beautify:pug include ./_sort-descending.pug

h3 Additional Sizes
p.-text
  | Chi also supports additional table sizes which can be configured by
  | applying the modifier class <code>-small</code> or <code>-large</code>.

h4 Small
:example(lang="html"):html-beautify:pug include ./_small.pug

h4 Large
:example(lang="html"):html-beautify:pug include ./_large.pug

h3 Selectable
p.-text Combine several different modifiers to create complex and beautiful tables.
:example(lang="html"):html-beautify:pug
  include ./_common.pug
  section.a-table
    div
      table
        thead
          tr
            th
              div.-lh1
                input.a-input(type="checkbox", id=`chk-1`)
                label(for=`chk-1`)
            th.-sorted.-descending
              div name
            th
              div ID
            th
              div last login
        tbody
          +str(`chk-1-01`, 'David Navarro', 'david', 'Dec 18, 2017 3:26 PM', true)
          +str(`chk-1-02`, 'Diego Sanjuan', 'diego', 'Dec 18, 2017 2:38 AM')
          +str(`chk-1-03`, 'Manuel Martin', 'manuel', 'Nov 5, 2017 10:15 AM')
