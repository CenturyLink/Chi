---
title: HTML Selects
---

each size in ['default', 'small', 'large']
  div(
    class=`test-${size}`
    class={
      'c-lh2': size == 'small'
    }
  )
    span.c-h2= `HTML Select / ${size}`
    table(cellpadding="0", cellspacing="0", width="600px")
      each state in ['initial', 'hover', 'active', 'focus', 'disabled']
        tr
          td
            small= `${state}`
          each style in ['default', 'danger']
            td
              .c-m1
                select(
                  class='c-input'
                  class={
                    [`c-input--${style}`]: style != 'default',
                    [`c-input--${size}`]: size != 'default',
                    [`c-input--${state}`]: state != 'initial' && state != 'disabled'
                  }
                  disabled=state=='disabled' ? 'disabled' : false
                )
                  option Select
                  option An option
                  option Another option
