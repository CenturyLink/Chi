---
title: Badges
---
script(type='module' src='../../js/ce/ux-chi-ce/ux-chi-ce.esm.js')
script(nomodule='' src='../../js/ce/ux-chi-ce/ux-chi-ce.js')

each type in ['solid', 'outline', 'flat']
  div(class=`test-${type}`)
    span.chi-h2= `Badge / ${type}`
    div
      each style in ['base', 'primary', 'secondary', 'danger', 'dark', 'muted', 'light']
        chi-badge(class='-mr--1', variant=`${type === 'solid' ? '' : type}`, color=`${style === 'base' ? '' : style}`, id=`badge-${type}-${style}` )= `${style}`
        chi-badge(class='-mr--1', variant=`${type === 'solid' ? '' : type}`, color=`${style === 'base' ? '' : style}`, id=`badge-icon-${type}-${style}` )
          chi-icon(icon='atom')
          span= style

h2 Badge Icons
.test-badge-icons
  .example.-mb--1
    each size in ['xs', 'sm', 'default']
      .p.-text--bold= size
        chi-badge(size=`${size === 'default' ? '' : size}`, class='-m--1', id=`badge-${size}`)
          chi-icon(icon='atom')
          span Badge
        chi-badge(size=`${size === 'default' ? '' : size}`, class='-m--1')
          span Badge
          chi-icon(icon='atom')
        chi-badge(size=`${size === 'default' ? '' : size}`, class='-m--1')
          chi-icon(icon='atom')
          span Badge
          chi-icon(icon='atom')

h2 Text Transform
.example.-mb--1
  each transform, text in { BADge: 'lowercase', badGE: 'uppercase', badge: 'capitalized' }
    .p.-text--bold= transform
      chi-badge(text-transform= transform, class='-m--1', id=`badge-${transform}`)= text
