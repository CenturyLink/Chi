---
title: Alert Toasts
---
- const sizes = ['h6', 'h5', 'h4', 'h3', 'h2', 'h1'];

each size in ['default', 'sm', 'lg']
  h4 Default / #{size}
  div(class=`test-toast-${size}`)
    each val, type in {success:'icon-circle-check', danger:'icon-circle-warning', warning:'icon-warning', info:'icon-circle-info', muted:'icon-circle-info'}
      .chi-alert.-toast.-m--2(class=`-${type} ${size === 'default' ? '' : '-' + size}`)
        div(class=`chi-alert__icon chi-icon -text--${type}`)
          svg
            use(xlink:href=`#${val}`)
        .chi-alert__content
            p.chi-alert__text=`This is ${type === 'info' ? 'an' : 'a'} ${type} alert`
        button(class='chi-alert__dismiss-button chi-btn -icon -close', aria-label='Close')
          .chi-btn__content
            .chi-icon
              svg
                use(xlink:href='#icon-x')
  h4 Titled / #{size}
  div(class=`test-toast-titled-${size}`)
    each val, type in {success:'icon-circle-check', danger:'icon-circle-warning', warning:'icon-warning', info:'icon-circle-info', muted:'icon-circle-info'}
      .chi-alert.-toast.-m--2(class=`-${type} ${size === 'default' ? '' : '-' + size}`)
        div(class=`chi-alert__icon chi-icon -text--${type}`)
          svg
            use(xlink:href=`#${val}`)
        .chi-alert__content
            p.chi-alert__title= type
            p.chi-alert__text=`This is ${type === 'info' ? 'an' : 'a'} ${type} toast alert`
        button(class='chi-alert__dismiss-button chi-btn -icon -close', aria-label='Close')
          .chi-btn__content
            .chi-icon
              svg
                use(xlink:href='#icon-x')
