---
title: Steps
---

- var steps = ['Create account', 'Choose number', 'Add seats', 'Choose services', 'Checkout'];

h4 Base
  .example.-mb--3(style="width:900px")
    .-p--3
      ul.a-steps.-mb--8
        each step in steps
          if step == 'Create account'
            li.-active
              a(href='#')= `${step}`
          else
            li
              a(href='#')= `${step}`
      ul.a-steps.-mb--8
        each step in steps
          if step == 'Choose number'
            li.-active
              a(href='#')= `${step}`
          else
            li
              a(href='#')= `${step}`
      ul.a-steps.-mb--8
        each step in steps
          if step == 'Add seats'
            li.-active
              a(href='#')= `${step}`
          else
            li
              a(href='#')= `${step}`
      ul.a-steps.-mb--8
        each step in steps
          if step == 'Choose services'
            li.-active
              a(href='#')= `${step}`
          else
            li
              a(href='#')= `${step}`
      ul.a-steps.-mb--8
        each step in steps
          if step == 'Checkout'
            li.-active
              a(href='#')= `${step}`
          else
            li
              a(href='#')= `${step}`
