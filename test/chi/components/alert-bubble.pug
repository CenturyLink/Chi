---
title: Alert Bubbles
---
h3 default
div(class=`test-default -w--100`)
  each val, type in {success:'icon-circle-check', danger:'icon-circle-warning', warning:'icon-warning', info:'icon-circle-info', muted:'icon-circle-info'}
    .m-alert.-bubble.-m--2(class=`-${type}`)
      div(class=`a-icon -sm--2 -text--${type}`)
        svg
          use(xlink:href=`#${val}`)
      = `this is a ${type} message`
    .m-alert.-bubble.-dismiss.-m--2(class=`-${type}`)
      .m-alert__content
        div(class=`a-icon -sm--2 -text--${type}`)
          svg
            use(xlink:href=`#${val}`)
        = `This is a ${type} alert`
      button(class='a-btn -icon -close -sm')
        .a-btn__content
          .a-icon
            svg
              use(xlink:href='#icon-x')
h3 large
div(class=`test-large -w--100`)
  each val, type in {success:'icon-circle-check', danger:'icon-circle-warning', warning:'icon-warning', info:'icon-circle-info', muted:'icon-circle-info'}
    .m-alert.-bubble.-lg.-titled.-m--2(class=`-${type} -lg`)
      .m-alert__content
        div(class=`a-icon -sm--2 -text--${type}`)
          svg
            use(xlink:href=`#${val}`)
        div
          h5.a-h5= type
          = `this is a ${type} message`
    .m-alert.-bubble.-lg.-titled.-dismiss.-m--2(class=`-${type} -lg`)
      .m-alert__content
        div(class=`a-icon -sm--2 -text--${type}`)
          svg
            use(xlink:href=`#${val}`)
        div
          h5.a-h5= type
          = `This is a ${type} alert`
      button(class='a-btn -icon -close -sm')
        .a-btn__content
          .a-icon
            svg
              use(xlink:href='#icon-x')
