---
title: Popover
scripts:
  - js/chi
---
script(type='module' src='../../js/ce/ux-chi-ce/ux-chi-ce.esm.js')
script(nomodule='' src='../../js/ce/ux-chi-ce/ux-chi-ce.js')

- var sides = ['top', 'right', 'bottom', 'left'];
- var moreSides = ['top', 'top-start', 'top-end', 'right', 'right-start', 'right-end', 'bottom', 'bottom-start', 'bottom-end', 'left', 'left-start', 'left-end'];

.test-base(style='width: 220px; height: 220px;')
  span.a-h2 Base
  .-position--relative
    section.m-popover.m-popover--top.-active.-position--absolute(aria-hidden="false", aria-label="Popover", role="dialog")
      header.m-popover__header
        h2.m-popover__title Popover!
      .m-popover__content
        p.m-popover__text This is a popover content.

each side in sides
  div(class=`test-${side}`, style='width: 330px; height: 220px;')
    span.a-h2=`Simple side ${side}`
    .-position--relative
      section.m-popover.-active.-position--absolute(aria-hidden="false", class=`m-popover--${side}`, aria-label="Popover", role="dialog")
        header.m-popover__header
          h2.m-popover__title Popover!
        .m-popover__content
          p.m-popover__text This is a popover content.
        .m-popover__arrow

each side in moreSides
  div(class=`test-more-${side}`)
    span.a-h2=`Popper side ${side}`
    .-d--flex.-justify-content--center.-align-items--center(style="margin: 120px 220px;")
      button(data-position=side, data-popover-content='<header class="m-popover__header"><h2 class="m-popover__title">Popover!</h2></header><div class="m-popover__content"><p class="m-popover__text">This is a popover content.</p></div>').a-btn.autostart Popover

div(class=`test-with-progress`, style='width: 450px; height: 220px;')
  span.a-h2=`Popover with progress`
  .-position--relative
    section.m-popover.m-popover--top.-active(aria-hidden="false", aria-label="Popover", role="dialog")
      header.m-popover__header
        h2.m-popover__title Popover title
      .m-popover__content
        | Current progress (75%)
        progress.-success(value='75', max='100', role='progressbar', aria-valuenow='75', aria-valuemin='0', aria-valuemax='100')
      .m-popover__arrow
script.
  document.addEventListener(
    'DOMContentLoaded',
    function () {
      var popovers = chi.popover(
        document.querySelectorAll('.autostart')
      );
      Array.prototype.forEach.call(popovers, function(popover){
        popover.show();
      });
    }
  );
